trigger:
  batch: true
  branches:
    include:
      - staging
  paths:
    include:
      - /node-app/node
        /node-app/manifest
        /node-app/pipelines.yml

pool:
  vmImage: ubuntu-latest

extends:
  template: acr-pipeline.yml